/**
 * Removido na Etapa F10 (Rich Pickers) — substituído por `IconPicker.tsx`,
 * que compõe o novo `RichPicker` em vez de reimplementar popover/grid do
 * zero. Ver `docs/analise-arquitetural-rich-pickers.md`, seção 3.
 *
 * Arquivo mantido vazio (em vez de removido) porque este ambiente não
 * consegue deletar arquivos do mount montado — nenhum código do projeto
 * importa mais `IconField` a partir daqui.
 */
export {};
