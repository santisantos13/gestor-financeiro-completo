# Dependências usadas só em desenvolvimento/teste - nunca em produção.
# `-r requirements.txt` garante que tudo que roda em produção também está
# disponível aqui, sem duplicar as versões.
-r requirements.txt
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
